# Core dependencies
fastapi==0.99.1
uvicorn[standard]==0.32.0
python-dotenv==1.0.1

# Database
sqlalchemy==2.0.36
psycopg[binary]==3.3.2
alembic==1.14.0

# Solver
ortools>=9.11

# Utilities
pydantic<2.0.0
# pydantic-settings removed (embedded in pydantic v1)

# Fuzzy Matching (for Normalization Agent)
fuzzywuzzy==0.18.0
# Note: python-Levenshtein is optional but speeds up fuzzy matching
# On Windows with Python 3.13+, it may fail to compile; fuzzywuzzy works without it

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
httpx==0.28.1
